# Discount Calculator

A tiny project used to test agent development workflows.

## Goal

Implement a discount calculator according to `SPEC.md`.

The implementation should be minimal, correct, tested, and avoid
unrelated refactoring.

## Run tests

```bash
python -m unittest